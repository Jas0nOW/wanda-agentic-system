# SSOT Brainstorm — Decisions to Lock

Answer these quickly (bullet answers). This becomes ADR inputs.

1) **CLI UX**: Fully interactive TUI, or minimal prompts + config file editing?
2) **Default install path**: single command (recommended) vs step-by-step?
3) **Secrets**: prefer env vars, encrypted config, or OS keychain?
4) **Update policy**: auto-check on start? auto-install next start? or manual only?
5) **Supported providers**: which are first-class defaults (OpenAI, Google, etc.)?
6) **Modes**: when should `wanda` route to voice vs code? (heuristics)
7) **Agent policy**: which agents are default-enabled, and which are opt-in?
