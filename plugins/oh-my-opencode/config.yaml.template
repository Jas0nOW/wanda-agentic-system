# ══════════════════════════════════════════════════════════════════════════════
# OH-MY-OPENCODE Configuration v1.0.3
# ══════════════════════════════════════════════════════════════════════════════

leader:
  name: "Sisyphus"
  model: "{{ORCHESTRATOR_MODEL}}"
  fallback: "{{ORCHESTRATOR_FALLBACK}}"
  prompt_file: "{{AGENTS_CONFIG_PATH}}"

agents:

  sisyphus:
    name: "Sisyphus"
    symbol: "🎯"
    layer: 2
    model: "{{ORCHESTRATOR_MODEL}}"
    triggers: ["always_active", "/plan"]
    
  prometheus:
    name: "Prometheus"
    symbol: "🏗️"
    layer: 3
    model: "{{ARCHITECT_MODEL}}"
    triggers: ["design", "architecture"]
    
  atlas:
    name: "Atlas"
    symbol: "👨‍💻"
    layer: 3
    model: "{{DEVELOPER_MODEL}}"
    triggers: ["code", "implement"]
    
  brainstormer:
    name: "Brainstormer1"
    symbol: "💡"
    layer: 1
    model: "kimi-k2.5-free"
    triggers: ["brainstorm"]
    
  librarian:
    name: "Librarian1"
    symbol: "📚"
    layer: 4
    model: "{{LIBRARIAN_MODEL}}"
    triggers: ["remember", "research"]
    
  audit:
    name: "Audit1"
    symbol: "🔍"
    layer: 3
    model: "{{AUDIT_MODEL}}"
    triggers: ["review", "debug"]
    
  writer:
    name: "Writer1"
    symbol: "✍️"
    layer: 4
    model: "{{WRITER_MODEL}}"
    triggers: ["docs"]

deactivated:
  - hephaestus
  - octto
  - commander
  - orchestrator
  - architect
  - developer
  - software_engineer

workflows:
  ralph_loop:
    description: "3-Phase Autopilot"
    phases:
      - agent: prometheus
        name: "Design"
      - agent: atlas
        name: "Implement"
      - agent: audit
        name: "Verify"
